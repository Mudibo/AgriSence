# Tests

This folder contains placeholder pytest stubs for the core model components. The tests are intentionally minimal at this stage and are meant to be expanded once the data pipeline and modeling implementation are more fully specified.

## Files

- `test_cleaning.py` — placeholder validation for cleaning utilities.
- `test_feature_engineering.py` — placeholder validation for feature generation logic.
- `test_inference.py` — placeholder validation for forecast-generation behavior.
- `__init__.py` — marks the package for import.
