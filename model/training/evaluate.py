"""Evaluation metrics and model-scoring utilities."""

from __future__ import annotations


def compute_mae() -> float:
    """Compute mean absolute error for a set of predictions."""
    # TODO: calculate MAE from actual and predicted values.
    return 0.0


def compute_rmse() -> float:
    """Compute root mean squared error for a set of predictions."""
    # TODO: calculate RMSE from actual and predicted values.
    return 0.0


def compute_mape() -> float:
    """Compute mean absolute percentage error for a set of predictions."""
    # TODO: calculate MAPE from actual and predicted values.
    return 0.0


def evaluate_model(model, X_test, y_test) -> dict:
    """Evaluate a trained model and return a dictionary of metrics."""
    # TODO: score the model, calculate metrics, and return a dict with metric names.
    return {}
