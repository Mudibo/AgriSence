# Artifacts

This folder is reserved for serialized model objects, evaluation outputs, and intermediate pipeline results. It is the staging area for trained models and saved forecast artifacts before they are reviewed or moved to a downstream storage layer.

## Files

- `.gitkeep` — keeps the artifacts directory in version control.
- `models/.gitkeep` — keeps the serialized model directory in version control.
